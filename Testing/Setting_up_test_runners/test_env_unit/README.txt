Check the env is set up otherise follow instruction
1. ensure that NodeJS is installed on your machine.
2. run npm install -g karma
3. run npm install -g karma-cli //to avoid getting 'karma' is not recognized as an internal or external command, operable program or batch file.
4. run npm install karma-ng-scenario //
5. run npm install karma-junit-reporter // for reports in xml format


1. To start unit tests run "karma start" from cli
2. As a result you should see Chrome to be launched; out put to appear in console; xml out putt appear in report folder